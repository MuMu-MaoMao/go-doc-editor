# Code Review 记录：文档数据库存储

- **日期**: 2026-06-06
- **审查人**: [开发者自我审查]
- **当前阶段**: Code Review ✅

---

## 审查清单检查

### Go 代码审查检查清单

#### 错误处理

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 错误使用 `%w` 包装 | ⚠️ 大部分使用 `%v` | 与项目现有风格一致（`user/store.go` 也使用 `%v`），非阻塞问题 |
| 没有使用 `_` 忽略错误 | ✅ | 仅 `rows, _ := result.RowsAffected()` — Go 标准模式 |
| `defer` 在错误检查后正确使用 | ✅ | `rows.Close()` deferred after error check |
| 资源有 `defer` 释放 | ✅ | 所有 rows 查询都有 defer Close |

#### 并发安全

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 共享状态互斥保护 | ✅ | 无需（docstore 无共享可变状态） |
| goroutine 无泄漏 | ✅ | 无新 goroutine |

#### 安全

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 路径安全校验 | N/A | **有意移除**：数据库存储天然消除路径穿越风险 |
| SQL 注入防护 | ✅ | 全部使用参数化查询 |
| 用户隔离 | ✅ | 所有查询 WHERE 包含 `username = ?` |

#### Go 惯例

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 包名小写单数 | ✅ | `docstore` |
| 导出符号有注释 | ✅ | Store, NewStore, ListFiles, ReadFile, SaveFile, DeleteFile, ErrFileNotFound |
| 与现有风格一致 | ✅ | 与 `user.Store` 模式完全一致 |

### 架构审查

| 检查项 | 结果 | 说明 |
|--------|------|------|
| Handler 层零改动 | ✅ | `file_handler.go` 未修改 |
| 前端零改动 | ✅ | `static/` 目录全部未修改 |
| 向后兼容 | ✅ | API 接口格式不变，config.json 兼容 |
| ADR 已创建 | ✅ | ADR-005-document-db-storage.md |
| readme 已更新 | ✅ | 目录结构、安全设计、数据库章节、版本历史 |
| 错误字符串兼容 | ✅ | `ErrFileNotFound` → "文件不存在" 与 handler 匹配 |

### SQL 审查

| SQL | 参数化 | 说明 |
|-----|--------|------|
| ListFiles | ✅ | `SELECT filename FROM documents WHERE username = ? ORDER BY filename ASC` |
| ReadFile | ✅ | `SELECT content FROM documents WHERE username = ? AND filename = ?` |
| SaveFile | ✅ | `INSERT ... ON DUPLICATE KEY UPDATE` (参数化) |
| DeleteFile | ✅ | `DELETE FROM documents WHERE username = ? AND filename = ?` |

### docstore.Store API 设计

| 方法 | 语义 | 正确性 |
|------|------|--------|
| ListFiles | 返回文件名列表（空数组非 null） | ✅ |
| ReadFile | 不存在返回 ErrFileNotFound | ✅ |
| SaveFile | upsert 语义 | ✅ |
| DeleteFile | 影响 0 行返回 ErrFileNotFound | ✅ |

---

## 审查发现

### 非阻塞问题（无需修复）

1. **`fmt.Errorf` 使用 `%v` 而非 `%w`**
   - 位置：`docstore/doc_store.go` 全部 error 包装
   - 当前项目约定：`user/store.go` 同样使用 `%v`，保持一致性
   - 建议：后续可统一升级为 `%w` 以支持 `errors.Is`/`errors.As`

2. **无单元测试**
   - 本次迁移属于架构变更，docstore 操作需要真实 MySQL 数据库才能测试
   - 现有项目 `user/store.go` 也未写单元测试（同模式）

### 无阻塞性问题

所有文件通过审查，无安全漏洞、无逻辑错误、无未处理错误路径。

---

## 最终结论

- [x] 代码通过自我审查
- [x] 架构一致
- [x] 安全风险已评估
- [x] 文档已更新
- [x] 可进入验收测试阶段
