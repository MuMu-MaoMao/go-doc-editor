# 实现日志：配置文件

- **日期**: 2026-06-06
- **当前阶段**: 实现完成 ✅

## 变更

**新建** `config.json`：
```json
{
    "port": ":3000",
    "storage": "C:\\doxreader",
    "mysql_dsn": "root:13579@tcp(localhost:3306)/godoxedit?...",
    "ai_key": "your-ai-key-here"
}
```

**修改** `internal/config/config.go`：
- Config 结构体增加 json 标签
- 新增 `findProjectRoot()` — 由源文件路径推算项目根目录
- 新增 `loadConfigFile()` — 读取 config.json
- 优先级：环境变量 > 命令行参数 > config.json

## 使用方式

```bash
# 直接启动（从 config.json 读取参数）
go run cmd/server/main.go --ai-key="真实key"
