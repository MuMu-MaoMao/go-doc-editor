# 功能需求 Log：配置文件替代命令行参数

- **日期**: 2026-06-06
- **当前阶段**: 需求提出 ✅

## 功能描述
将 `--ai-key`、`-mysql-dsn`、`-port`、`-storage` 等参数存入配置文件 `config.json`，减少命令行长度。

## 设计要点
1. 新建 `config.json` 在项目根目录，存放数据库密码等参数
2. AI Key 用假值占位（防泄露），运行时通过 `--ai-key` 覆盖
3. 优先级：配置文件（默认）→ 命令行参数（覆盖）→ 环境变量（覆盖）
